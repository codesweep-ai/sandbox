{
  "id": "SBX-041",
  "title": "On Linux every lender start copies the host's cs-sandbox into the group, though the image already carries a matching one",
  "type": "improvement",
  "severity": "low",
  "status": "in-progress",
  "foundBy": "maintainer, while reviewing the fix for SBX-040, 2026-09-15",
  "opened": "2026-09-15",
  "resolved": null,
  "stint": "2026-09-15 lender stage race",
  "evidence": {
    "commits": [],
    "integrated": [],
    "verified": null
  },
  "resolution": null,
  "details": "When `create` starts a group's lender container on Linux, `lenderBinary` in internal/cli/lend_create.go picks the running cs-sandbox. `lenderBox.stage` then copies that binary, about 10 MB, into `<group>/.lender/`, and the container runs the copy.\n\nFor a released cs-sandbox the copy buys nothing. The binary names an image built from its own revision, and image/Containerfile installs the cs-sandbox that made it. The container therefore already holds a matching lender, which is what macOS runs today.\n\nThe copy is needed in two cases only. The slim image the test tiers boot carries no cs-sandbox at all, and a developer changing the lender wants their build without rebuilding the image. Both can name a binary with `CS_SANDBOX_LENDER_BIN`, and the Makefile already does for the tiers. The copy is also the step whose shared temporary name caused the race in SBX-040.",
  "notes": [],
  "links": [
    "SBX-040"
  ]
}
