{
  "id": "SBX-006",
  "title": "CS_SANDBOX_ENGINE selects the engine and appears in no document",
  "type": "defect",
  "severity": "low",
  "status": "closed",
  "foundBy": "session agent, while comparing the CS_SANDBOX_* variables in the code against the ones in the docs",
  "opened": "2026-08-15",
  "resolved": "2026-08-16",
  "stint": "2026-08-16 publication readiness",
  "evidence": {
    "commits": [
      "98c42cc"
    ],
    "integrated": [],
    "verified": "`grep -rn CS_SANDBOX_ENGINE *.md` returned nothing before and now returns MANUAL.md's Environment table, second group, with the podman default the flag parser applies."
  },
  "resolution": null,
  "details": "`internal/cli/create.go:65` defaults the `--engine` flag from `CS_SANDBOX_ENGINE`. Grepping the markdown for that name returns nothing, in MANUAL.md or anywhere else.\n\nSo a user on a Linux host with KVM who wants Podman has to pass `--engine podman` on every create, or read the source to learn that one export would do it. The variable is the closest thing the tool has to a user-level engine default, and it is invisible.\n\nMANUAL.md is where a flag's meaning lives, by the table in CONTRIBUTING.md, so the entry belongs beside `--engine` there.",
  "notes": [],
  "links": [
    "SBX-010"
  ]
}
