{
  "id": "SBX-050",
  "title": "build --local-sandbox fails from a git worktree, and so do two unit tests",
  "type": "defect",
  "severity": "low",
  "status": "closed",
  "foundBy": "session agent, running `make check` from a git worktree while fixing SBX-044",
  "opened": "2026-09-20",
  "resolved": "2026-09-25",
  "stint": null,
  "evidence": {
    "commits": [
      "1b95c11"
    ],
    "integrated": [],
    "verified": "As the record reproduces it: in a worktree of a scratch clone at 3b3e213, whose .git is a file, `go test ./internal/cli/ -run TestLocalModuleProxy` passes both proxy tests. GitHub CI's `ci` passed on 3b3e213, once the nested sandbox smoke job was run again: its microVM had stopped answering."
  },
  "resolution": "The local module proxy zips from the checkout that holds the repository's git directory, which `git rev-parse --git-common-dir` names. A worktree shares its commits, so a build from one works. `--local-sandbox` is gone, and `build` packs its own commit through this proxy.",
  "details": "`make check` failed in a checkout made with `git worktree add`. Two tests were the failures, `TestLocalModuleProxyInstallsAnUnpushedRevision` and `TestLocalSandboxMountsTheProxyAndOverridesGOPROXY`. The error from both was `could not find a recognized version control system`. The same commit passed from an ordinary clone.\n\nThe local module proxy zips the repository with Go's module zip code, which looks for a `.git` directory. In a worktree `.git` is a file that points at the main checkout. `cs-sandbox build --local-sandbox` takes the same path, so it fails for anyone who works from a worktree.\n\nTo reproduce, run `git worktree add ../wt origin/main`, then `go test ./internal/cli/ -run TestLocalModuleProxy` in `../wt`.\n\nResolving the worktree's real git directory before zipping would fix it. `git rev-parse --git-common-dir` names it.",
  "notes": [],
  "links": []
}
